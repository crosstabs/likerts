# Existing collection only. Never prefix management credentials with NEXT_PUBLIC_.
# Local operator routes are DISABLED until you explicitly opt into loopback-only testing.
# LIKERTS_EXAMPLE_LOCAL_OPERATOR=1
LIKERTS_EXAMPLE_ORIGIN=http://127.0.0.1:4330
LIKERTS_API_URL=http://127.0.0.1:8080
LIKERTS_COLLECTION_ID=replace-with-existing-collection-id
LIKERTS_COLLECTION_TOKEN=replace-with-collection-credential
LIKERTS_TOKEN=replace-with-server-only-responses-read-credential
