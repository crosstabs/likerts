# Likerts task list

Current plan, 9 September 2026. Checked items identify verified local or hosted work at the stated boundary, not overall production readiness. Unchecked items remain open. Owner labels identify workstreams, not currently running agents. No delivery dates or effort estimates are committed.

**Product:** customer-controlled embedded surveys; free onboarding with one nonrenewing 1,000-response workspace grant, then US$0.01 per accepted response from paid credits. The minimum US$5 purchase buys 500 responses. No subscription or provisioning fee. All five SDKs at first launch. No hosted survey links, distribution, email campaigns, enterprise SSO or dedicated deployments.

## Completed prototype work

- [x] **DONE-01 — Core backend:** survey drafts, immutable publication, collection configurations and authoritative answer validation.
- [x] **DONE-02 — Interface parity:** ten implemented API operations exposed through MCP and Rust CLI; OpenAPI and contract checks.
- [x] **DONE-03 — Five SDK foundations:** Web, React Native, iOS, Android and Flutter clients/renderers with platform checks.
- [x] **DONE-04 — Question expansion:** NPS, yes/no, scale labels and multiple-choice selection limits across the backend, tools and SDKs.
- [x] **DONE-05 — Compatibility:** baseline schema v1 and expanded v2; unsupported versions rejected; published schemas remain immutable.
- [x] **DONE-06 — Integration checks:** cross-workspace denial, invalid answers unbilled, concurrent identical retries counted once, and closed-collection retry behavior.

## Engineering decisions and validation

Engineering has selected the defaults in [ENGINEERING-DECISIONS.md](ENGINEERING-DECISIONS.md). Checked decisions do not mean their integrations are implemented. Engineering owns the remaining validation gates.

- [x] **DEC-01 — Account and authorization provider** · Identity. Production Clerk at `clerk.likerts.com` is configured for passwordless email OTP with usernames/passwords disabled; passkeys remain an optional paid upgrade. Likerts owns workspace roles, grants and scoped service credentials behind a provider-neutral verifier. Fresh-user OTP/recovery and live browser-session removal remain ID-01 acceptance gates; OAuth-specific acceptance remains ID-02.
- [x] **DEC-02 — Free onboarding and settlement policy** · Billing. One verified workspace receives one nonrenewing 1,000-response promotional grant without a card. Thereafter, US$0.01 accepted responses consume prepaid balance; a US$5 purchase adds 500 credits. No subscription, provisioning fee or monthly commitment. Acceptance pauses at zero balance, and append-only entries preserve grant, purchase, consumption, refund and reversal history. See PRICING.md.
- [x] **DEC-03 — Retention and service limits** · Product + backend. Defaults recorded: 90-day raw response retention, 24-hour exports/deletion processing, seven-day backups, bounded payloads and traffic, finite collection and retry lifetimes. Implement and measure these in DATA/BE/OPS tasks. Merchant financial-record retention must be established before paid launch.
- [ ] **DEC-04 — Runtime validation** · Infrastructure. Current launch topology is Render Singapore API/worker/MCP, Neon Singapore Postgres through Vercel Marketplace, Vercel control plane and private Blob exports. Hosted role, lifecycle, export and sandbox payment checks pass. Sustained hosted capacity, recovery objectives and actual provider bills remain OPS-01–03 gates; AWS is not part of this launch.

## P0 — Durable and isolated service

- [x] **BE-01 — PostgreSQL storage** · Backend. Migrations and repositories persist every core record. The isolated PostgreSQL test now verifies reconnect and real backend-process restart persistence; an injected failure after response insertion proves the transaction cannot leave a response without its usage entry.
- [x] **BE-02 — Database tenant isolation** · Security + backend. Forced RLS now covers every tenant table; runtime grants are separate from migrations and every repository operation uses transaction-local workspace or collection-capability context. A `NOINHERIT NOBYPASSRLS` role test proves unscoped reads return no rows, cross-workspace writes fail and context clears when the same pooled connection is reused.
- [x] **BE-03 — Durable idempotency and accounting** · Backend. Response and immutable one-cent usage commit together. Concurrent retries, changed-payload conflicts, injected rollback, backend termination/restart and a discarded-then-retried acknowledgement return one durable receipt and one usage entry. Receipt retention and terminal expiry behavior are defined in ENGINEERING-DECISIONS.md for DATA-03 implementation.
- [x] **BE-04 — Collection lifecycle and caps** · Backend. Collection creation accepts a future expiry of at most 90 days and a positive response cap; revocation is irreversible, while closure and expiry preserve prior receipts. Row locks make response-cap acceptance atomic, workspace locks bound active collections to 100 across concurrent create/reopen operations, and restricted-role PostgreSQL tests cover cap, closure, expiry, revocation and active-limit races.
- [x] **BE-05 — Management retry safety** · Backend + interfaces. Survey and collection creation require a bounded idempotency key. The payload digest and original response commit with the resource; identical concurrent and post-restart retries return the original ID and collection credential, while changed payloads return HTTP 409 through API, MCP and CLI. Restricted-role PostgreSQL tests prove one resource under eight concurrent attempts for each operation.
- [ ] **ID-01 — Real accounts and workspace access** · Identity. Production Clerk custom-domain/passwordless configuration and the deployed bootstrap bridge are complete. A real fresh user must still complete OTP signup, recovery, one-time workspace/grant creation and live-session membership removal. Configuration and rendered sign-in are not evidence that this full customer journey passed.
- [x] **ID-01A — Local workspace-authorization gate** · Identity. Likerts-owned membership and role checks are durable and tenant-isolated under the restricted PostgreSQL role. A real API process proves membership creation/removal and service-credential issue/use/revoke take effect on the next request. Locally signed Clerk-shaped RS256 tests cover signature, exact issuer, audience, client identity and expiry validation. Production passwordless configuration is complete; fresh-user email OTP/recovery and live-session removal remain ID-01 gates. Passkeys are optional.
- [x] **ID-01B — Local browser control-plane bridge** · Identity + backend. A separate Clerk-session verifier requires exact issuer/audience/expiry/subject and binds `azp` to an exact allowed browser Origin. One deterministic personal workspace, owner membership and the fixed promotional grant are created atomically once; replay requires live membership and cannot restore revoked ownership. The control plane reads usage with an in-memory session token and no cookies. Explicit owner review can approve only configured OAuth clients and exact scopes for 30 days; session tokens never become OAuth capability tokens and no first-use grant exists. Local JWT, CORS, browser build, PostgreSQL and contract gates pass; fresh-user OTP/session/removal acceptance remains ID-01 hosted evidence.
- [x] **ID-01C — Production identity configuration** · Identity + infrastructure. `clerk.likerts.com` is configured as the production Clerk custom domain, passwordless email OTP is enabled and usernames/passwords are disabled. The production control plane is deployed against this identity configuration. A real fresh-user OTP/recovery journey is explicitly separate in ID-01.
- [ ] **ID-02 — Scoped OAuth and service credentials** · Identity + interfaces. Self-service scoped agent credential creation/listing/revocation is deployed, and hosted child credential issuance/revocation passes. API/MCP/CLI support these credentials. Complete fresh-user browser issuance and the real Codex/Claude/CLI connection journey; provider OAuth custom scopes, consent, refresh-family revocation and signing-key rotation remain unverified and must not be advertised as complete.
- [x] **ID-02A — Local scoped-credential gate** · Identity + interfaces. Restricted-role tests prove the intersection of token scope, durable grant scope and current membership, including expired/revoked/wrong-audience/over-scoped OAuth grants plus expired/revoked/under-scoped service credentials. CLI tests cover authorization-server metadata, required scope advertisement, S256 PKCE, RFC resource binding, state binding, refresh rotation, private atomic storage and logout cleanup; the contract gate keeps all management operations aligned across API/MCP/CLI. Clerk consent, refresh-family revocation, JWKS rotation and real CLI/MCP client flows remain ID-02 sandbox gates.
- [x] **ID-02B — Self-service scoped agent credentials** · Identity + interfaces. The browser control plane exposes scoped credential creation, listing and revocation using the authenticated browser bridge; API, MCP and CLI accept tenant-bound service credentials. The API rejects an explicit `X-Likerts-Workspace` that differs from the service credential's tenant, binding remote MCP workspace URLs as documented; unit and real PostgreSQL/API tests cover matching and mismatching selections. Production acceptance permits the exact workspace through direct API and MCP, denies a mismatch with 403, and returns 401 through both paths after workspace tombstoning. Fresh-user browser issuance and actual Codex/Claude connection acceptance remain ID-02; service-credential support does not establish provider OAuth consent/refresh completion.
- [ ] **SEC-01 — Collection security** · Security + SDKs. Bound public collection capabilities, rate-limit abuse, distinguish client metadata from trusted context and specify any signed-context flow. Verify browser CORS/origin behavior without treating origins as authentication. Depends on BE-04 and ID-02.
- [x] **SEC-01A — Local collection-security gate** · Security + SDKs. Collection capabilities remain schema/submission-only and digest-stored; exact HTTPS origins are configured separately from authentication. A durable per-collection minute limiter returns 429 before validation/billing, while invalid attempts remain unbilled. API/MCP/CLI configuration, real Web preflight/allowed/denied origin behavior, CORS error headers, restricted-role RLS and rate exhaustion pass. Client metadata is explicitly untrusted and v1 has no signed-context field; the future typed flow is specified in COLLECTION-SECURITY.md. Production IP/WAF behavior and ID-02 provider evidence remain gates for SEC-01.
- [ ] **SEC-02 — Audit and secret handling** · Security. Record sensitive management operations without logging credentials or response contents; restrict support access. Verify logs and error paths do not expose customer data. Depends on ID-02.
- [x] **SEC-02A — Local secret, audit and object-store gate** · Security + backend + infrastructure. Collection capabilities are HMAC-derived from a runtime-only 32-byte key and only SHA-256 digests persist; retry and audit rows exclude credentials and response contents. Restricted PostgreSQL grants make audit history append-only, and error/log/metrics paths expose bounded classifications. The deployed private Vercel Blob adapter passes a hosted export/download/deletion roundtrip. A bounded scan of 500 production Render API/MCP/worker log records found no credential, database URL, email or response-payload patterns. Ongoing drains/alerts, provider access-policy review, support break-glass enforcement, denied-operation coverage and rotation remain SEC-02.

## P0 — Useful results and actual billing

- [x] **DATA-01 — Paginated response retrieval** · Backend + interfaces. Response pages use an immutable acceptance sequence and a cursor-bound snapshot, limit and collection/time filters. Submission/list advisory locks make the first boundary include all earlier commits and exclude later acceptance without serializing submissions with each other. Restricted-role PostgreSQL tests prove deterministic retry, tenant isolation and no gaps or duplicates while responses are accepted between pages; API, MCP and CLI integration verifies query encoding, structured errors and contract parity.
- [x] **DATA-02A — Local export lifecycle and recovery** · Backend + interfaces. Idempotent CSV/JSON jobs preserve their snapshot boundary, cap selection at 100,000 responses/64 MiB, require scoped downloads, and expire after 24 hours. Migration 0019 adds five-minute durable leases, fenced snapshot/completion/failure, four-minute execution bounds and distinct per-attempt object keys. Authorized status polling or same-key retry recovers interrupted execution; new creation reaps abandoned tenant reservations. Restricted PostgreSQL tests prove competing replacement claims, stale-worker denial, tenant isolation, lost-pool recovery and a replacement HTTP API completing the same job. Memory and real CLI/MCP/Web lifecycle checks pass. See EXPORT-RECOVERY.md and scripts/check-export-leases.sh.
- [ ] **DATA-02 — Hosted export recovery readiness** · Backend + operations. Normal private Blob export creation/download and deletion-triggered export revocation pass in `infrastructure/render/hosted-lifecycle-evidence.json`. Prove actual API termination and cross-replica recovery, orphan-attempt cleanup, original 24-hour expiry and revocation under hosted failures. Recovery remains customer-request driven; no autonomous cross-tenant scheduler is claimed. Deployment and refreshed local restore evidence both cover migration 0022.
- [x] **DATA-03 — Deletion and retention** · Backend + operations. Raw response erasure preserves only the capability-keyed retry digest, receipt fields and immutable one-cent usage; every erasure records a tenant-scoped deletion event and revokes affected exports. Bounded retention batches enforce the workspace's 1–90-day raw-data setting and 24-hour exports. Workspace tombstoning revokes collection credentials, removes identity and management retry records, redacts survey content and blocks subsequent authorization. Memory/API/MCP/CLI integration and restricted-role PostgreSQL tests prove explicit and scheduled erasure, unchanged accounting, identical/changed retry behavior, tenant isolation, export-object removal and full account lifecycle. Seven-day backup expiry and deletion replay before restored traffic are documented in DATA-LIFECYCLE.md; the managed restore drill remains OPS-03.
- [x] **BILL-01A — Usage and atomic-cap foundation** · Billing. Usage reports lifetime/current-month responses and cents, durable limits and a blocking reason. New submissions serialize on a workspace billing lock after idempotency lookup, then commit acceptance and one-cent usage together; memory and restricted-role PostgreSQL races prove exactly one final unit is accepted, concurrent excess work is rejected and the winning retry remains readable. This historical foundation is extended by BILL-01B below.
- [x] **BILL-01B — Local promotional and prepaid ledger** · Billing + backend. Migration 0018 adds an append-only, forced-RLS ledger; one fixed 1,000-credit workspace grant, promo-first atomic consumption, zero-balance rejection, idempotent owner-only recorded adjustments and ID-only audit. Usage exposes promotional/paid balances and consumption through API/MCP/CLI. Restricted runtime cannot mint purchases; the webhook worker cannot read credits. Memory and restricted PostgreSQL tests pass for exhaustion, concurrency, restart, rollback, refund/reversal, tenant denial and deletion/recreation denial. Gross one-cent usage remains immutable and credit-funded responses are excluded from legacy postpaid settlement. See BILLING-LIMITS.md and scripts/check-response-credits.sh.
- [ ] **BILL-01 — Verified onboarding and credit launch controls** · Billing + backend. Local accounting and hosted one-response promotional consumption pass. Finish fresh-user verified signup, duplicate-person/workspace abuse controls and 80/90/100% balance thresholds/notifications. Same-workspace retries and tombstones cannot recreate the grant; duplicate-person grant prevention remains unverified. Complete hosted load/accounting and financial-record retention acceptance before paid launch.
- [ ] **BILL-02 — Live Stripe credit purchase and reconciliation** · Billing. US$5-or-greater Checkout initiation/status works through API/MCP/CLI. Hosted Stripe test mode proves a US$5 payment credits exactly 500 responses and a successful full US$5 refund removes those 500 paid credits while preserving all 1,000 promotional credits and leaving no debt; the temporary workspace was tombstoned. See `infrastructure/render/hosted-refund-evidence.json`. Migration 0021 supplies cumulative refund/dispute adjustments; migration 0022 is deployed. Remaining gates are live merchant activation/credentials and production payment acceptance, hosted dispute/partial/spent-credit refund cases beyond deterministic tests, and financial-retention policy. Optional owner-controlled automatic top-up is a separate future feature, not a blocker for explicit prepaid purchases.
- [x] **IF-01A — Current capability contract gate** · Interfaces. All 40 current product operations have API/MCP/CLI parity, authoritative OpenAPI-derived MCP input/output schemas, 80 validated example files and scoped usage documentation in contracts/CAPABILITIES.md. The gate compares all 53 explicit HTTP registrations with OpenAPI, reviews transport exemptions, checks 74 schemas and 22 API error codes, rejects missing/stale examples and executes every operation through real MCP transport and the compiled CLI against a validating HTTP fixture. Safe structured tool errors preserve status/operation without echoing upstream bodies. Existing real-backend CLI/MCP/Web lifecycle integration and focused adapter/CLI tests pass. CI runs the gates unconditionally.
- [ ] **IF-01 — Complete release capability parity** · Interfaces. IF-01A verifies the currently implemented surface. Keep this release gate open until any remaining release capabilities are implemented and pass the same typed-schema, examples, errors and transport checks; hosted/release verification remains separate. New routes or omitted interfaces/examples fail the local gate.

## P0 — All five SDKs ready together

These are parallel launch requirements, not sequential platform launches.

- [x] **SDK-01 — Shared SDK behavior contract** · SDK lead. Contract v1 defines local validation feedback, renderer/client lifecycle and cancellation, HTTPS-only production transport, rejected redirects, 15-second default timeouts, caller-controlled idempotent retries, callback meaning and schema-version rejection. All five suites consume one nine-scenario fixture; `scripts/check-all-sdks.sh` builds and executes the complete contract gate.
- [x] **SDK-02 — Safe remote configuration** · Backend + SDKs. Publish and collection creation persist customer-declared target/version/schema support and reject an operation when any installed group lacks the actual schema version. All five SDKs export their declaration and implement a five-minute cache with explicit refresh, eviction on refresh failure and immutable binding checks. Memory, restricted-role PostgreSQL and real CLI/MCP/Web integration tests cover mixed v1-only/current installations and frozen bindings.
- [ ] **SDK-WEB — Web release quality** · Web. Accessibility/keyboard checks, CSP integration, styling/localization, lifecycle cleanup and real-browser integration against the backend. Meet SDK-01 and SEC-01.
- [ ] **SDK-RN — React Native release quality** · React Native. Host-app integration, Android/iOS device tests, accessible controls, styling/localization and supported framework-version matrix. Meet SDK-01 and SEC-01.
- [ ] **SDK-IOS — iOS release quality** · iOS. Sample application, native UI/device tests, accessibility, styling/localization and supported OS matrix. Meet SDK-01 and SEC-01.
- [ ] **SDK-ANDROID — Android release quality** · Android. Sample application, instrumented/device tests, accessibility, styling/localization and supported OS matrix. Meet SDK-01 and SEC-01.
- [ ] **SDK-FLUTTER — Flutter release quality** · Flutter. Sample application, Android/iOS integration tests, accessibility, styling/localization and supported Flutter-version matrix. Meet SDK-01 and SEC-01.
- [x] **SDK-03A — Local artifact and clean-install gate** · SDK lead. Frozen versions 0.0.1 (schemas 1/2), 0.0.2 (schemas 1–4) and 0.0.3 (schemas 1–5) exist for all five SDKs with SHA-256 manifests, changelogs, installation guides and customer-controlled trigger examples. `scripts/check-sdk-release.sh` builds and installs packaged files in fresh consumers: Web npm/TypeScript/public and offline exports/DOM cleanup; React Native npm/TypeScript/Babel with RN 0.86.3/React 19.2.3 and its offline subpath; extracted SwiftPM tests plus public advanced/offline/SwiftUI consumer compilation; extracted Maven AAR/POM/Gradle metadata and advanced/offline types in a new Android APK; and extracted Flutter pub/analyze/widget checks with advanced/offline APIs. All five pass. Evidence and artifacts are in `releases/0.0.1`, `releases/0.0.2` and `releases/0.0.3`; earlier manifests and artifact hashes still verify. The gate refuses to overwrite any successful manifest. `sdks/INSTALLATION.md` defines the workflow. No registry publication occurred.
- [x] **SDK-03B — Direct preview distribution** · SDK lead. Checksummed 0.0.3 artifacts for Web, React Native, iOS, Android and Flutter plus a checksummed Rust CLI source bundle are live at `https://likerts.com/downloads/`. All six were downloaded from the production edge and matched `SHA256SUMS`; the CLI bundle also installs and runs from a fresh extraction with Cargo. This provides a usable preview installation path without registry credentials.
- [ ] **SDK-03 — Public package and installation readiness** · SDK lead. Direct preview distribution passes. Establish registry accounts/namespace ownership and release credentials, publish coordinated supported versions to the intended npm/Swift/Maven/pub distribution channels, and verify unqualified public installation from clean customer projects. Platform release tasks and the coordinated decision remain open; npm packages are private and Flutter retains `publish_to: none`.

## P0 — Operate and validate the service

- [x] **OPS-01A — Local deployment artifact and gate** · Infrastructure. A locked Rust OCI build provides a non-root runtime and separate migration binary. The isolated container gate covers restricted runtime grants, repeatable migrations, unsafe-role rejection, read-only filesystem, scoped authentication, clean SIGTERM replacement, durable data, unscoped RLS denial and append-only audit grants. Local CI validation passes; hosted GitHub Actions execution remains blocked by account billing/spending limits. Deployment/rollback runbook: infrastructure/README.md.
- [x] **OPS-01B — Historical optional AWS infrastructure gate** · Infrastructure. Credential-free CDK staging/production synthesis and five isolation assertions pass. This is retained alternate-provider work; AWS provisioning, S3/KMS/CloudWatch acceptance and AWS rollback are not launch requirements for the selected Render/Vercel/Neon service.
- [x] **OPS-01C — Render/Vercel deployment and role gate** · Infrastructure. The protected Singapore Render environment runs two API instances, a separate callback worker, MCP gateway and inert owner-only migration base. Neon Launch Singapore supplies PostgreSQL through Vercel Marketplace; migrations through 0022 are deployed. Hosted exact-grant checks pass: runtime has no memberships/schema-create privilege, and worker cannot read responses. Vercel serves `likerts.com` with production Clerk `clerk.likerts.com`; private Blob, Stripe test mode and Upstash are connected. API/MCP health and unauthenticated denial pass. The unused Render Postgres resource was removed.
- [ ] **OPS-01 — Reproducible staging and delivery** · Infrastructure. Normal hosted create/publish/collect/retrieve/private-export/delete/revoke lifecycle is complete. A production candidate with no runtime diff was promoted and health-checked, then Render restored the recorded compatible revision in 36.619 seconds; health and unauthenticated denial passed after restoration. An isolated credential-free service then proved a failing candidate reaches `update_failed` while Render keeps the last healthy revision serving; the rehearsal service was deleted. Remaining acceptance is private Blob recovery after API termination across replicas, customer-owned HTTPS callback/DNS denial tests and the accepted worker egress boundary. Resolve GitHub Actions billing/spending limits and prove the hosted delivery checks before relying on CI promotion.
- [x] **OPS-02A — Local load and shared-service model** · Performance + product. The resource-bounded durable container benchmark covers paced 100 RPS, bursts, duplicate retries, invalid/unauthorized/oversized traffic, response-cap races, CSV/JSON exports and physical/WAL storage growth. Usage equals accepted responses and rejects are unbilled. economics/SHARED-ECONOMICS.md combines this evidence with 18 official Singapore AWS rates and explicit settlement/support/maintenance assumptions. The snapshot predates the webhook worker and excludes its hosting/storage/egress costs; those require OPS-02 modeling. No 429s were observed in the measured snapshot; this does not close SEC-01.
- [x] **OPS-02B — Launch-provider planning model and control plane** · Performance + product. A deterministic Render/Vercel/Neon/Clerk model includes two API instances, callback worker, MCP gateway, migration cron, workspace fees, representative Neon usage, private Blob planning rates, optional passkey upgrade, one-time 1,000-response grant and US$5 purchases. Production control plane and Clerk domain are deployed, and hosted Stripe sandbox checkout/refund passes. These establish planning and functional evidence, not sustained capacity or profitable margins.
- [ ] **OPS-02 — Hosted capacity and economics validation** · Performance + product. A bounded production rehearsal now passes 260 attempts: 211 unique accepted responses and cents, exact promo-only accounting, duplicate/invalid/unauthorized/cap-race invariants, paced 5 RPS p95 206 ms and ten-concurrent burst p95 417 ms, followed by workspace tombstoning and credential denial. Evidence: `infrastructure/render/hosted-load-evidence.json`. Vercel reports US$0.101529 effective and US$0.028879 billed for the `likerts` project during 1–9 September 2026, mostly build CPU; this is a partial period and excludes Render, Neon, Clerk and Stripe. Marketplace balance endpoints returned no Neon or Stripe balance information. Sustained Render/Neon capacity, multi-replica placement/failover, export/callback load and egress, complete provider bills and conversion/support demand remain unverified. Add activation/conversion cohorts, abandoned credits and duplicate-workspace abuse before claiming margins.
- [x] **OPS-03A — Local recovery and monitoring gate** · Operations. Separate-token internal Prometheus metrics are wired and verified through Rust tests and the release container; route labels exclude IDs and health/metrics traffic. Five alert rules pass Prometheus configuration and behavior tests. The refreshed PostgreSQL restore preserves 28 forced-RLS tables and all 22 migrations, 18 audit rows, four credit entries and the two-cent gross ledger; post-backup response/workspace deletions are replayed and old credentials, collection capabilities and exports are denied. Restored callback endpoints and queues are quarantined, and worker customer-data denial survives restore. Failed transactional DDL rolls back and altered migration checksums stop promotion. Evidence is in `infrastructure/recovery/local-evidence.json`: 172,458 backup bytes and 1,969 ms restore/replay/verification for the tiny local fixture. The promotional balance remains 998 credits; restoring and reading usage cannot reissue onboarding. Runtime credit mutation and worker credit-read denials survive. Hosted execution remains unverified.
- [ ] **OPS-03 — Hosted recovery and monitoring activation** · Operations. Use the current Neon database and Render/Vercel service: prove configured seven-day Neon retention and managed PITR/restore; independently durable deletion-journal replication and lag alerts; private Blob expiry/revocation; managed service/database/storage alarms and real notification delivery; identity/payment reconciliation before restored traffic; callback timeout/restart/concurrency and restore quarantine. Opening the Neon Marketplace recovery console currently stops at required Marketplace-account email verification, so no managed restore was executed. Local restore evidence now matches deployed migration 0022; measure hosted RTO/RPO separately. Render Postgres-specific PITR timing and AWS recovery requirements are superseded. Local logical-dump timings are not production commitments.
- [ ] **REL-01 — Cross-platform release rehearsal** · QA + all workstreams. The shared local five-platform rehearsal, hosted core lifecycle, bounded hosted load and US$5 Stripe sandbox purchase/full refund are complete. `release/acceptance/` now supplies the guarded production customer runner and exact operator evidence checklist for OTP, grant replay, API/CLI/MCP, Codex, Claude, Web SDK lifecycle, payment/refund and deletion. Execute it with a fresh controlled inbox and complete supported host/device acceptance on all five platforms; include hosted failure/recovery paths and live payment acceptance. Keep all five launch surfaces.
- [x] **REL-01A — Local cross-platform release rehearsal** · QA + SDKs. A fresh PostgreSQL-backed service runs the shared fixture through Web, React Native, Swift, Android and Flutter, checking retrieval/export, idempotency, conflicts/validation without billing, promotional consumption, throttling, deletion/revocation and workspace tombstoning. Evidence: `release/local-rehearsal-evidence.json`. Hosted lifecycle and checkout/refund evidence supplement this gate; they do not substitute for fresh-user identity, managed recovery or supported device acceptance.
- [x] **REL-02 — Launch decision** · Product + engineering. `LAUNCH-DECISION.md` records GO for an invite-only developer preview and HOLD for public paid launch. The decision is based on hosted lifecycle/payment evidence, local security/recovery/package gates and explicit remaining owner/provider acceptance. Only verified preview claims may be published.

- [x] **REL-01B — Hosted core lifecycle and sandbox payment acceptance** · QA + backend. Against the deployed API, scoped credentials are issued/revoked; surveys publish with all-five-SDK declarations; collection configuration and one accepted response roundtrip; private Blob export downloads; response deletion revokes the export; collection revocation and workspace tombstoning invalidate access. One promotional credit is consumed. A separate US$5 Stripe test purchase/full refund preserves the promotional balance and returns paid credits to zero. Evidence: `infrastructure/render/hosted-lifecycle-evidence.json` and `infrastructure/render/hosted-refund-evidence.json`.
- [ ] **REL-03 — Customer support, legal and privacy readiness** · Product + onboarding. A six-file review pack in `docs/legal/` drafts privacy, terms, prepaid credits/refunds, subprocessors and support/incidents with an owner/legal checklist. Establish the real contracting entity, jurisdiction, contacts, staffing, refund/credit-expiry decisions, executed provider terms and verified retention before removing draft markers and publishing. Confirm merchant financial-record retention and public pricing wording before accepting paying customers.

## P1 — Next survey expansion

Planned after the current four-feature expansion. These are backlog items, not additional agreed first-launch gates; if promoted into launch, they must work across all five SDKs and management interfaces.

- [x] **Q-01 — Conditional visibility** · Survey engine + SDKs. Schema v3 adds one bounded `visibleWhen` predicate with scalar equality, multiple-choice inclusion and answered operators. Backend draft validation rejects unknown/self references, incompatible operators/values and dependency cycles; hidden sources count as unanswered, comparison operators require an answer, and answered text is trimmed. Hidden answers are recursively discarded before validation/persistence and hidden required questions are not required. The shared fixture passes backend immutable-store and real API/MCP/CLI/Web integration plus Web, React Native, Swift, Android and Flutter renderer suites. See `contracts/CONDITIONAL-VISIBILITY.md`.
- [x] **Q-02 — Pages and branching** · Survey engine + SDKs. Schema v4 adds a bounded page partition and ordered forward branches that reuse v3 predicates. Draft validation rejects incomplete/duplicate partitions, unknown or non-forward destinations, future-dependent conditions and duplicate branches. The backend reconstructs the reached route, discards answers from skipped/hidden questions before requiredness and persistence, and freezes page definitions in published schemas and exports. All five SDKs provide matching route/progress/Back/answer-change behavior and their built-in renderers validate the current page and submit only routed answers. The PostgreSQL integration and `scripts/check-pages-branching.sh` cross-platform gate pass. See `contracts/PAGES-AND-BRANCHING.md`.
- [x] **Q-03 — Other and exclusive options** · Survey engine + SDKs. Schema 3 adds exact structured Other text and exclusive None selection semantics, shared Rust/SDK fixtures, Unicode-aware bounds, and preserved schema 1/2 publication immutability. `scripts/check-question-features.sh` passes all five SDK suites plus real API/MCP/CLI/Web publication, response validation, idempotent 1-cent billing and export checks. Android API35 instrumented and Flutter widget flows verify Other text clearing and exclusive replacement.
- [x] **Q-04 — Additional presentation presets** · SDKs. Schema 3 stars and dropdown presets retain numeric-scale and single-choice stored values across all five native renderers. Shared validator fixtures, localized/accessible controls and real Android/Flutter/Web/RN interaction tests pass with the Q-03 combined gate. Existing scale labels and NPS/yes-no presets remain compatible. This establishes tested semantics and controls, not independent accessibility certification.

## Later — Evaluate before committing

- [x] **LATER-01A — Local response-event delivery gate** · Backend + interfaces + infrastructure. Customer-configured, initially disabled endpoints receive signed ID-only events through an atomic PostgreSQL outbox and a separate restricted worker. Bounded durable retry/lease recovery, event deduplication, protected idempotent key issuance/rotation, replay, disable/revoke, erasure and expiry are verified. All seven management operations have typed API/MCP/CLI parity. Real restricted-role PostgreSQL, signed loopback HTTP wire tests, wrong-master-key failure, negative worker privileges, the release container, callback restore quarantine and credential-free staging/production CDK assertions pass. See infrastructure/webhooks/README.md and infrastructure/webhooks/local-evidence.json.
- [ ] **LATER-01 — Hosted response-event delivery acceptance** · Operations. LATER-01A implements and verifies the local feature. Remaining acceptance requires an owned public HTTPS receiver, deployed DNS/egress enforcement, timeout/restart/concurrent-worker rehearsal, delivered worker alarms and Render restore quarantine evidence. Execute with OPS-01/03; no hosted callback or delivery availability claim is established by local synthesis/tests.
- [x] **LATER-02 — Advanced question families:** schema 5 defines ranking as a complete option-ID permutation, bounded single/multiple matrices as row-to-column mappings, and exact-total constant-sum allocation. Rust authoring/submission validation, immutable storage, OpenAPI, API/MCP/CLI and all five SDK models/renderers share the same fixtures. Renderers provide explicit rank movement, stacked matrix rows and announced remaining allocation; invalid submissions remain unbilled. `scripts/check-advanced-questions.sh` passes the authoritative backend, contract, all-SDK and real interface round trip. Frozen SDK 0.0.3 artifacts support schemas 1–5 and expose advanced plus offline APIs in five independent clean consumers. See `contracts/ADVANCED-QUESTIONS.md` and `releases/0.0.3/manifest.json`.
- [x] **LATER-03 — Durable offline collection:** all five SDKs expose explicit bounded queues with immutable idempotency envelopes, oldest-first caller-triggered flush, cancellation, exact retry/terminal mapping, local expiry, quarantine and safe aggregate status. Web uses real IndexedDB plus a non-extractable AES-GCM WebCrypto key; native SDKs require injected Keychain/Keystore-backed authenticated-encryption storage adapters and ship no plaintext fallback. Only a valid same-collection accepted one-cent receipt removes a record; local queue work never bills. `scripts/check-offline-queue.sh` and the full cross-SDK gate pass. Native host storage durability, backup exclusion and hardware key protection remain the integrating application's responsibility. See `contracts/OFFLINE-COLLECTION.md` and `sdks/OFFLINE-QUEUE.md`.

## How to execute

Continue unchecked gates using their stated dependencies and current evidence. The BE foundation, SDK behavior contract, Q-01–04 and durable offline SDK work are complete; do not restart those tasks from the historical implementation order. Recheck the combined 0.0.3 release artifact after advanced-question integration. Fresh-user identity, live merchant activation, public package delivery and remaining Render/Neon/Blob operational gates require their own evidence before public launch. Completed hosted lifecycle and sandbox payment/refund checks must not be repeated as unfinished implementation. Follow ENGINEERING-DECISIONS.md for the selected operating defaults.

A task is complete only when its stated acceptance checks pass and relevant contract, interface, SDK and documentation changes are included. Keep this list synchronized with BUILD-STATUS.md; do not mark the entire service ready because a prototype test passes.
