{
  "name": "@likerts/control-plane",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-public-seo.mjs --check && node scripts/build.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
