# Likerts Flutter example

This application demonstrates host-owned placement, scrolling, Material theming, submit lifecycle and completion UI around `LikertsSurvey`.

From this directory, use `flutter run`, or run `../../../../scripts/check-flutter-matrix.sh` from any directory for the package and native build gates.
