# Pre-release archives

The versioned archives in this directory predate the free community edition and are retained only as historical build artifacts. They use an older response-receipt contract and are not linked or deployed by the current website.

Build SDKs from the current source tree until the first community-edition release is tagged.
